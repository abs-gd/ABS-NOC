<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ABS NOC{% endblock %}</title>
    <link rel="stylesheet" href="/scss/compiler.php?v={{ "now"|date("U") }}">
</head>
<body>
<header>
    <nav>
        <button id="nav-toggle" aria-label="Toggle menu">☰</button>
        <ul id="nav-menu">
            <li><a href="/">Home</a></li>
            <li><a href="/servers">Servers</a></li>
            <li>
                <form method="POST" action="/logout">
                    <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                    <button type="submit">Logout</button>
                </form>
            </li>
            <li><button id="dark-mode-toggle">🌙 Dark Mode</button></li>
        </ul>
    </nav>
    </header>

    <main>
        {% block content %}{% endblock %}
    </main>

{{ jsfile('darkModeToggle.js') }}
{{ jsfile('navToggleMobile.js') }}

</body>
</html>
